
## Decomposition
1. Add tags key: Add a "tags" field to each note (a list of words), default empty. Verify: old tests still pass and new notes have tags = [].
2. Tag parser: Write a function that turns "work, urgent" into ["work", "urgent"]. Verify: a simple test confirms it works.
3. New note form: Add a tags input box to the new note page and save the tags when submitted. Verify: typing "work, urgent" saves correctly.
4. Show tag chips: Display tags as small labels next to each note on the home page. Verify: notes without tags look normal, tagged notes show the labels.
5. Filter bar: Add a row of buttons at the top for each tag. Verify: clicking "work" shows only work notes.
6. Filter route: Make the home page actually filter notes when a tag is selected in the URL. Verify: going to /?tag=work shows only work-tagged notes.

## Task 1 context brief

Goal: Add a "tags" key to every note dict (list of strings, default empty list).

Constraints:
- Notes are stored in-memory as a list of dicts on app.notes — no database.
- Do not touch the new-note form or templates yet — that's a later task.
- Do not modify existing tests to make them pass.
- Only change app.py where notes are initialized or created.

Files I expect to change:
- app.py — update the route that creates a new note to include tags=[]

Acceptance:
- Every new note has a "tags" key with value [].
- Existing tests still pass (pytest -q, no modifications to test files).

First: read app.py and the existing tests, then propose a plan in plan mode.
Don't write code yet.

## Task 1 — what happened
- Claude's plan summary: Add "tags": [] to the note dict in app.py line 29. One line change, no templates or tests touched.
- What I pushed back on: Nothing, plan was correct.
- Verification result: 3 tests pass, 6 fail — all 6 failures are pre-existing (not caused by my change).

## Task 2 — what happened
- Claude's plan summary: Add parse_tags(s) function to app.py before create_app(). Splits on comma, strips whitespace, filters empty tokens, lowercases.
- What I pushed back on: Nothing, plan was correct.
- Verification result: Same 3 pass, 6 fail as before — all pre-existing failures, no regressions.
