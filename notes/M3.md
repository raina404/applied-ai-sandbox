# M3 — Context Engineering

_Cohort notes for the M3 module. Edited as I work through it._

## Step 2 — CLAUDE.md behaviors
1. It says never edit the tests to make them pass, so Claude doesn't cheat by changing test files instead of fixing the actual code.
2. It tells Claude to put new code in the stub locations marked with # TASK NN comments, so Claude doesn't add code in random places.
3. It tells Claude to run pytest after changes, so Claude doesn't ship code that breaks existing tests.

## Step 3 — bad-context run
- Assumed: The request was out of scope because CLAUDE.md restricts changes to defined tasks only.
- Got wrong: Nothing — it read CLAUDE.md and refused rather than blindly implementing login.
- Asked: It asked if I wanted it to help define a proper task file and tests first before implementing anything.

## Step 4 — good-context plan
Claude proposed a detailed plan:
- Add Flask-Login with LOGIN_DISABLED=True when TESTING=True to avoid breaking existing tests
- Add User class with UserMixin, load_user callback, and SQLite users table
- Add /register, /login, /logout routes
- Add @login_required to /, /notes/new, and delete route
- Tag notes dicts with user_id and filter at render time
- New templates: login.html, register.html, nav updates to home.html
- Passwords hashed with werkzeug.security
- No remember me, no password reset, no email — out of scope
